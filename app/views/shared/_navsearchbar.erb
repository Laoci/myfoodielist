<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
    <%= link_to root_path, class: "navbar-brand1" do %>
      <a href="#" class="navbar-logo">
      <img src="./logo.png" width="150" alt="My foodie list logo">
    </a>
    <% end %>

    <%= render 'shared/search' %>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <%= link_to "Home", root_path, class: "nav-link" %>
            </li>
            <%- if current_user %>
            <li class="nav-item dropdown">
                <%= image_tag "https://source.unsplash.com/random/100x100", class: "avatar
                dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" },
                'aria-haspopup': true, 'aria-expanded': false %>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                    <%= link_to "My lists", user_lists_path(current_user), class:
                    "dropdown-item" %>
                    <%= link_to "Log out", destroy_user_session_path, method: :delete, class:
                    "dropdown-item" %>
                </div>
            </li>
        <% else %>
            <div>
            <li class="nav-item">
                <!-- Button trigger modal -->
                <button
                    type="button"
                    class="nav-link"
                    data-toggle="modal"
                    data-target="#exampleModalB">
                    Log in
                </button>
            </li>
            </div>
            <div>
            <li class="nav-item">
                <!-- Button trigger modal -->
                <button
                    type="button"
                    class="nav-link"
                    data-toggle="modal"
                    data-target="#exampleModalA">
                    Sign up
                </button>
            </li>
            </div>
            <% end %>
        </ul>
    </div>
</div>
<%= render "devise/registrations/modal" %>
<%= render "devise/sessions/modal" %>
