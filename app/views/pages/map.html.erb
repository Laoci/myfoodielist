<%= stylesheet_pack_tag 'application' %>
<%= render 'shared/navsearchbar' %>

<h1>My Foot Trail</h1>

<!--  Map wrapper -->
<div class="container">
  <div class="row">
    <div class="d-flex justify-content-center">
      <div id="map" style="width: 1100px; height: 450px;" data-markers="<%= @markers.to_json %>" data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
    </div>
  </div>
  <br>
  <div class="row">
    <div class="d-flex justify-content-start">
      <%= link_to "Back to Dashboard", user_lists_path(current_user), class:"btn btn-flat my-1" %>
    </div>
  </div>
</div>
